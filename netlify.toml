# netlify.toml
[build]
  command = "npm run build && npm run export"
  publish = "out"

# Indica que Netlify debe usar la rama develop para desplegar
[context.develop]
  command = "npm run build && npm run export"
  publish = "out"
